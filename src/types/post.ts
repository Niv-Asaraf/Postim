export type SummaryPost = {
  id: number;
  title: string;
  description: string;
  primaryImageUrl: string;
  author: string;
  publishedAt: string;
};
export type PostCategory = {
  id: number;
  name: string;
};
export type RelatedPostType = {
  id: number;
  title: string;
  primaryImageUrl: string;
};
